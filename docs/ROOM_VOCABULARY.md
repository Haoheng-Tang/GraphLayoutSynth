# Canonical Room Vocabulary

The default config (`configs/generic_building.yaml`) defines a medium-detail
healthcare/building vocabulary. This document is the reference for what the
backend recognizes, what it generates by default, and how hierarchy is
represented.

## Three vocabulary tiers

1. **Allowed canonical vocabulary** — every room type the backend
   recognizes: `allowed_node_types` in the active config. These are valid in
   Program Requirements payloads, appear in the room-type catalog
   (`GET /program-requirements/room-types`), and are exposed to Claude in
   instruction-guided variant prompts.
2. **Default essential generated mix** — the smaller subset the default
   grammar actually generates without special instructions (see
   `room_type_counts` and the grammar rules). Default graphs stay moderately
   detailed rather than containing every possible type.
3. **Optional room types** — allowed canonical types that are *not*
   generated by default. Program Requirements can request them and
   instruction-guided variants can generate them; they simply require a
   config variant whose grammar creates them.

The catalog exposes the tier per entry: `generated: bool` and
`tier: "generated" | "optional"`, derived from the active config's grammar
rules through the same config-source resolution as the suggestion sampler.
Only `generated` types can appear in `/suggest-next-room` results.

## Design rules

- **Graph node types are flat.** Every canonical ID is a single PascalCase
  name (`CleanUtility`, `Stair`), never a nested path
  (`ClinicalSupport.CleanUtility` is invalid). Generated graph JSON always
  carries flat type IDs.
- **Hierarchy lives in `semantic_node_groups`.** Categories such as
  clinical support, administration, or vertical circulation are groups over
  flat types, surfaced through the ConfigContract and prompt context — they
  are not node types themselves.
- **`Stair`/`Elevator` are graph nodes only.** Adding vertical circulation
  types does not implement egress, code-compliance, or travel-distance
  logic; generated graphs remain research prototypes with graph-only
  proxies.
- **Program Requirements should use catalog IDs.** The catalog endpoint is
  the source of truth for canonical IDs; instruction-guided rules must use
  exact canonical IDs (misspellings fail deterministic validation).

## Canonical room types

| ID | Meaning | Default mix |
| --- | --- | --- |
| `PatientRoom` | Patient room or bed room. | yes (~24) |
| `OnStageCorridor` | Primary clinical/patient-facing corridor. | yes (4) |
| `OffStageCorridor` | Staff/service/back-of-house corridor. | yes (1) |
| `NurseStation` | Staff observation/work node. | yes (2) |
| `TeamRoom` | Staff collaboration room. | optional |
| `Office` | Administrative office. | optional |
| `ConferenceRoom` | Conference or meeting room. | optional |
| `MedicationRoom` | Medication storage/preparation room. | yes (2) |
| `CleanUtility` | Clean clinical utility room. | yes (2) |
| `SoiledUtility` | Soiled utility room. | yes (2) |
| `NourishmentRoom` | Nourishment or pantry support room. | optional |
| `LinenRoom` | Linen storage/support room. | optional |
| `EquipmentRoom` | Equipment storage/support room. | yes (2) |
| `StorageRoom` | General storage room. | yes (2) |
| `RespiratoryTherapyRoom` | Respiratory therapy support/storage room. | optional |
| `PPEStorage` | PPE storage/support room. | optional |
| `ConsultRoom` | Consultation room. | optional |
| `StaffLounge` | Staff lounge. | yes (1) |
| `StaffToilet` | Staff toilet/restroom. | yes (1) |
| `PublicToilet` | Public toilet/restroom. | yes (1) |
| `VisitorLounge` | Visitor lounge. | optional |
| `FamilyRoom` | Family room. | optional |
| `Dayroom` | Patient dayroom. | optional |
| `DiningRoom` | Patient/family dining room. | optional |
| `Stair` | Stair/vertical circulation node. | yes (2) |
| `Elevator` | Elevator/vertical circulation node. | yes (1) |
| `MEPRoom` | Mechanical/electrical/plumbing service room. | yes (1) |
| `ElectricalRoom` | Electrical service room. | optional |
| `MechanicalRoom` | Mechanical service room. | optional |
| `TelecomRoom` | Telecom/IT service room. | optional |
| `UtilityRoom` | General utility support room. | yes (1) |
| `Kitchen` | Kitchen/support food-service room. | optional |

Structural types `BuildingFloor` and `Zone` exist during generation but are
never user-facing program room types.

## Removed legacy types

`Corridor`, `ClinicalSupport`, and `StaffSupport` are no longer canonical:
they were removed from `allowed_node_types`, the semantic groups, the
catalog, and the visualization palette. Circulation is generated as
`OnStageCorridor`/`OffStageCorridor`, and the detailed support types replace
the two coarse support types. Circulation is a *declared* property: the
`corridor` semantic group is the source of truth for "is this a corridor?"
across validators and metrics, with the old name-token rule kept only as a
fallback for configs that declare no corridor group.

Imported floorplans may still carry legacy labels. Request validation
accepts unknown `rooms[].type` strings, so such payloads keep working — the
labels simply cannot be semantically matched, so those anchors produce empty
suggestions and the frontend uses its local fallback. There is no migration
or alias layer.

Conceptual names that are *not* canonical node types (represent them via
groups or map them to existing types): `Administration`, `ClinicalSupport`,
`StaffSupport`, `PatientSupport`, `PublicAmenity`, `Infrastructure`,
`UnitClerk`, `ChargeRN`, `TeamStation`, `Touchdown`, `Charting`,
`SupportAlcove`, `UnitSpecific`, `Balcony`, `EMS`. For example,
team-station/touchdown/charting concepts map to `NurseStation` or
`TeamRoom`.

## Semantic groups

`semantic_node_groups` in the default config defines the hierarchy:
`room_like`, `corridor`, `patient`/`patient_care`, `administration`,
`clinical_support`, `staff_support`, `patient_support`, `public_amenity`,
`vertical_circulation`, `building_service`, and `support`. The room-type
catalog is derived from `room_like` ∪ `corridor`; validators, preflight
corridor checks, and prompt context consume groups through the
ConfigContract.

**Semantic groups are the declared source of role-based behavior in both
repos.** Backend-side, group membership decides circulation
(`is_corridor_node_type`), review-summary support categories, and ranking's
support metric. Frontend-side, each room type's groups are published on the
room-type catalog as `groups`, and NextRoomPredictor resolves role-driven
behavior — corridor auto-extension, default room depth — from that field.

Neither side may infer role from spelling. Matching a substring in the type
ID happens to work for `OnStageCorridor`/`OffStageCorridor`, but it
misclassifies `NurseStation` (a support room) and fails completely for a
variant that names its circulation `Spine`, `Racetrack`, or `Hallway`. Those
names carry no "corridor" token yet still report `"groups": ["corridor"]`,
which is precisely why the field exists. To give a type a new role, add it
to a group in the config; do not rename the type and do not add a computed
role flag to the API.

## Example Program Requirements with new types

```json
{
  "schemaVersion": 1,
  "program": {
    "roomMix": {
      "PatientRoom": {"min": 20, "target": 22, "max": 24},
      "Stair": {"min": 2, "target": 2, "max": 2},
      "Elevator": {"min": 1, "target": 2, "max": 2},
      "MEPRoom": {"min": 1, "target": 1, "max": 2},
      "UtilityRoom": {"min": 1, "target": 2, "max": 3},
      "CleanUtility": {"min": 1, "target": 2, "max": 3},
      "MedicationRoom": {"min": 1, "target": 1, "max": 2}
    }
  }
}
```

Optional types (for example `Kitchen`, `FamilyRoom`, `TelecomRoom`) validate
the same way; generating them requires an instruction-guided or manual
config variant whose grammar creates them.

## Troubleshooting: catalog shows the old vocabulary

The catalog, Program Requirements validation (without an explicit
`baseConfigPath`), and `/suggest-next-room` all resolve the same config
source per request. If the frontend still shows the old four-type
vocabulary after this expansion:

1. **An old activated variant is pinned.** With
   `GRAPHLAYOUTSYNTH_GRAMMAR_MODE=active_variant`, all three endpoints
   correctly serve the *activated variant's* vocabulary — a variant
   proposed before the vocabulary expansion keeps its old
   `allowed_node_types` forever. Check `GET /grammar-variants` for the
   active record and its creation date; re-propose a variant from the
   updated base config and activate it (takes effect on the next request,
   no restart), or unset the grammar mode to serve the static default.
2. **The frontend cached the catalog response.** The backend does not set
   cache headers; if the frontend fetches the catalog once at startup, it
   must refetch after a variant activation or a backend config change.

`curl http://127.0.0.1:8000/program-requirements/room-types` shows what the
backend is actually serving — its `source` and `configPath` fields name the
resolved config.
